[project]
name = "vocml-featurizers"
version = "0.1.0"
description = ""
authors = [
    {name = "Logan Snider",email = "logop963@uw.edu"}
]
packages = [{include = "vocml_featurizers", from = "src"}]
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
    "numpy (>=2.3.1,<3.0.0)",
    "rdkit (>=2025.3.3,<2026.0.0)",
    "transformers (>=4.53.2,<5.0.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
