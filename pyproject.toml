[tool.poetry]
name = "vocml-featurizers"
version = "0.1.0"
description = ""
authors = ["Logan Snider <logop963@uw.edu>"]
readme = "README.md"
packages = [{include = "vocml_featurizers", from = "src"}]

[tool.poetry.dependencies]
python = ">=3.12,<3.15"
numpy = ">=2.3.1,<3.0.0"
rdkit = ">=2025.3.3,<2026.0.0"
transformers = ">=4.53.2,<5.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
